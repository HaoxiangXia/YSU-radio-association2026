{
  "name": "radio-association",
  "version": "1.0.0",
  "description": "无线电爱好者协会信息展示系统",
  "packageManager": "bun@1.x",
  "scripts": {
    "start": "cd backend && uv run uvicorn app:app --host 0.0.0.0 --port 5000",
    "dev": "cd backend && uv run uvicorn app:app --reload --host 0.0.0.0 --port 5000",
    "export:admissions": "bun scripts/export-admissions.js",
    "init": "bun server/initDB.js"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {}
}
